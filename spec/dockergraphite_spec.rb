require "spec_helper"

describe Dockergraphite do
  it "has a version number" do
    expect(Dockergraphite::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
